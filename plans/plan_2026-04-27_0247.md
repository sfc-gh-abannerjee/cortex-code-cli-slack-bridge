---
created: 2026-04-27T02:47:26.288Z
session: 40301dde-a9fe-4150-abb2-6e949067dad5
working_directory: /Users/abannerjee/Apps/cortex-code-cli-slack-bridge
---

## Implementation Plan: 7 Enhancements

### Hook Event Names (confirmed from HOOKS.md)
- Session end: `SessionEnd` (separate from `Stop`)
- All-tools PostToolUse: `"matcher": ".*"`
- Non-tool events (SessionStart, SessionEnd): omit `matcher`
- Stdin always includes: `session_id`, `hook_event_name`, `cwd`

---

### Phase 0 — Test Infrastructure
1. Add `[project.optional-dependencies]` dev group (`pytest>=7.4`, `pytest-mock>=3.12`) to `pyproject.toml`
2. Add `[tool.pytest.ini_options]` section
3. Create `tests/__init__.py`

### Phase 1 — Baseline Tests (write first, all expected to pass)
- `tests/test_notify.py` — verify `MSG_TYPE_COLORS` keys, `chat_postMessage` call shape
- `tests/test_bridge.py` — verify user-filter guard, session routing
- `tests/test_config.py` — verify existing token config functions

---

### Feature 1 — Rich Color-Coded Status Messages (bug fix)
**Test first**: assert `chat_postMessage` is called with `attachments=[{"color": ..., "blocks": [...]}]`  
**Fix**: In `notify.py`, wrap blocks in `attachments` list with the `color` key from `MSG_TYPE_COLORS`

### Feature 2 — PostToolUse Progress Hook
**Test first**: mock stdin JSON with `tool_name`/`session_id`, assert `coco-notify` is invoked  
**Implement**:
- Create `~/.snowflake/cortex/hooks/slack-progress.sh` — reads `tool_name` + `session_id` from stdin JSON via `jq`, enforces 3-min cooldown via `/tmp/coco_progress_${session_id}` flag file
- Update `hooks.json`: add PostToolUse entry `{"matcher": ".*", "hooks": [...]}`
- Note significant tools to filter (Bash, Edit, Write, sql_execute, Task)

### Feature 3 — SessionEnd Summary Hook
**Test first**: mock stdin JSON with `session_id`/`reason`, assert `coco-notify` is invoked  
**Implement**:
- Create `~/.snowflake/cortex/hooks/slack-session-end.sh` — reads `session_id`/`reason`, sends summary notification
- Update `hooks.json`: add `SessionEnd` entry (no matcher)

### Feature 4 — Reply Threading
**Test first**: assert `chat_postMessage` is called with `thread_ts` when flag provided  
**Implement**:
- `config.py`: add `get_last_ts(session_id)` and `set_last_ts(session_id, ts)`
- `notify.py`: add `--thread` CLI flag; pass `thread_ts` to `chat_postMessage`
- `bridge.py`: save inbound message `event["ts"]` via `set_last_ts` when DM arrives from `target_user`
- Update SKILL.md: document `--thread` flag usage

### Feature 5 — `/status` Slash Command
**Test first**: mock slash command body, assert response contains uptime/session info  
**Implement**:
- `bridge.py`: register `@app.command("/status")` handler — returns bridge pid, uptime, active sessions
- SKILL.md: add manual step for user to add `/status` slash command in Slack dashboard

### Feature 6 — Inbox Acknowledgment
**SKILL.md only** — add instruction: after reading inbox, CoCo sends `coco-notify "Acknowledged: {message_preview}"` and calls `coco-bridge ack {session_id}`

### Feature 7 — Multi-Session Selector
**Test first**: assert `sessions` lists registered sessions, `switch` updates active session  
**Implement**:
- `config.py`: add `register_session(session_id, label)`, `get_sessions()`, `set_active_session(session_id)`, `get_active_session()`
- `coco-bridge`: add `sessions` (list) and `switch <session_id>` subcommands
- `bridge.py`: route incoming DMs to active session's inbox, fall back to most-recent
- Update SKILL.md: document multi-session workflow

---

### Order of execution
Features implemented sequentially with `pytest` run after each feature. User commits manually (GPG signing requires Beyond Identity).

---

## User Context

but commit without gpg signing after each measurable addition